# Core API & Server
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic>=2,<3
python-dotenv==1.0.1
python-multipart>=0.0.9
Pillow>=10.3

# Audio / Whisper (lokal, Python-Bindings für whisper.cpp)
sounddevice==0.4.7
pywhispercpp>=1.1.0
# Optional: WebRTC VAD (nur wenn Build/Plattform passt)
# webrtcvad==2.0.10
# oder vorgebaute Wheels per Setup-Script installieren (empfohlen)

# Style pipeline (stabile, kompatible Pins mit Wheels)
numpy==2.0.1
scipy==1.12.0
scikit-image==0.24.2
scikit-learn==1.5.1
opencv-python==4.10.0.84
imageio==2.34.1
tifffile==2024.8.30
lazy-loader==0.4
networkx==3.2.1

# Build-Werkzeuge (nur falls nötig; manche Systeme brauchen sie nicht zur Runtime)
setuptools>=68
wheel>=0.41
